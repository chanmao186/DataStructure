#include <iostream>
#include <vector>
#include <unordered_map>
#include <set>
using namespace std;

int main() {
	/*set<int> s;
	vector<int> v;
	unordered_map<int, set<int>::iterator> m;
	m[5] = s.insert(5).first;
	m[6] = s.insert(6).first;
	m[7] = s.insert(7).first;
	m[8] = s.insert(8).first;
	//m[5] = v.insert(v.end(),5);
	v.push_back(6);
	v.push_back(7);
	v.push_back(8);
	v.push_back(9);
	v.push_back(10);
	
	v.erase(find(v.begin(),v.end(),8));
	s.erase(m[6]);
	for (auto &i : v) {
		cout << i << " ";
	}
	for (auto& i : s) {
		cout << i << " ";
	}		  */
	vector<int> a;
	for (auto x : a) {
		cout << x;
	}
	return 0;
}